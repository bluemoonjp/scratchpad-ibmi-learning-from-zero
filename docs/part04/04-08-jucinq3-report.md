# 04-08 帳票と受注照会の帳票版 JUCINQ3

> 所要時間: 75分(長め)/ 前提レッスン: 04-07 / 目標番号: 3 / 観測方法: `WRKSPLF` / 道具: 5250(SEU)/ 同時接続数: 5250×1 / 作る・変えるオブジェクト: `<USER>1/R0408A` / DBVER: 1 / 依存するプローブ: なし / PTF 依存: なし / 容量の目安: わずか

## ゴール

- 編集コードで、数値の見やすい表示形式を選べる。
- `CHAIN`・`READ`・条件分岐を組み合わせて、実用的な帳票プログラムを書ける。
- 受注照会の帳票版(`JUCINQ3`、通し例3)を作れる。

## ウォームアップ

<details><summary>前回の復習</summary>

1. `CHAIN` の結果標識(見つからないとき ON)は何桁目?
2. F 仕様書31桁目に `K` を書かないと、`CHAIN` の Factor 1 はどう解釈されようとする?

答え: 1. 54〜55桁目(HI) 2. 相対レコード番号

</details>

## なぜ学ぶか

**ここまでの部品(`CHAIN`・`READ`・`IFxx`・`GOTO`/`TAG`)を組み合わせれば、実用的な帳票プログラムが書けます。** 03-09 で CL 版(`JUCINQC`)を作った「得意先の受注一覧」を、ここでは RPG III の帳票として作ります(通し例3。`JUCINQ` コマンドの CPP を、この先 RPG III 版に差し替えていくことになります)。あわせて、04-02 で予告した**編集コード**を詳しく扱います。

## 新出

- 編集コード(数値の表示形式。1〜4、およびゼロのときの表示)
- 複数のファイル(`TOKUIM`・`JUCHUM`)を1つのプログラムで扱う

## 説明

### 編集コードの一覧

O 仕様書38桁目の編集コードは、数値フィールドの**印刷時の見た目**を決めます。

| 編集コード | 桁区切り(,) | 小数点 | 値がゼロのとき |
|---|---|---|---|
| (空白) | なし | なし(整数部のみ) | 0が並ぶ |
| 1 | あり | あり | 空白 |
| 2 | あり | あり | `0.00` のように0が残る |
| 3 | なし | あり | 空白 |
| 4 | なし | あり | 0が残る |

`1`〜`4` は「桁区切りの有無」と「ゼロのときに空白にするか0を残すか」の組み合わせです。実機で `1738.00`(小数2桁の数値)にそれぞれ試したところ、次のようになりました。

| 編集コード | 表示結果 |
|---|---|
| 1 | `1,738.00` |
| 3 | `1738.00`(桁区切りなし) |

また、値が `0.00` のとき、編集コード `1` では**空白**になり、編集コード `2` では**0が残ります**(04-02 の `R0402A` で使った `1` は、この「ゼロなら空白」という性質を利用していました)。

**J・K・L・M という編集コードもあり、それぞれ 1・2・3・4 と同じ規則に、先頭に `$` を付けます**(通貨表示用)。この教材では扱いません。

### 複数のファイルを1つのプログラムで扱う

```text
FTOKUIM  IF  E           K        DISK
FJUCHUM  IF  E                    DISK
```

F 仕様書は、必要なだけ複数書けます。`TOKUIM`(得意先マスター、`CHAIN` でキー・アクセス)と `JUCHUM`(受注マスター、`READ` で順に読む)を、両方入力ファイルとして宣言しています。

## 実演

1. `WRKMBRPDM FILE(<自分のユーザー名>1/QRPGSRC)` で `R0408A`(`RPG`)を作る。
2. 次を、桁位置に注意しながら入力する(`src/qrpgsrc/r0408s.rpg` と同じ内容です)。

   ```text
   ....+....1....+....2....+....3....+....4....+....5....+....6....+....7....+....8
         * R0408A - JUCINQ3: order inquiry report for one customer.
        H
        FTOKUIM  IF  E           K        DISK
        FJUCHUM  IF  E                    DISK
        FQSYSPRT O   F     132            PRINTER
        C                     MOVEL'C00001'  CUST    6
        C           CUST      CHAINTOKUIM               99
        C   99                MOVEL'NOTFOUND'TOKNM
        C           LOOP      TAG
        C                     READ JUCHUM                   98
        C   98                GOTO ENDLP
        C           JUTOK     IFEQ CUST
        C                     EXCPT
        C                     ENDIF
        C                     GOTO LOOP
        C           ENDLP     TAG
        C                     SETON                     LR
        OQSYSPRT E
        O                         TOKNM     30
        O                                   32 '  '
        O                         JUNO      38
        O                                   40 '  '
        O                         JUDATE    48
   ```

   得意先コード `CUST`(ここでは `'C00001'` に固定)を `TOKUIM` から `CHAIN` で探し、得意先名を取得します。次に `JUCHUM` を先頭から順に `READ` し、**得意先コードが一致する注文だけ**を印刷します(03-09 の CL 版 `JUCINQC` と、同じ絞り込みロジックです)。

3. コンパイルして `CALL PGM(R0408A)` を実行し、`WRKSPLF` で次のように印刷されることを確認する(02-06・03-09 で確認した結果と一致します)。

   ```text
   ACME TRADING CO                 J00001  20260901
   ACME TRADING CO                 J00003  20260905
   ```

## 演習

1. `'C00001'` を `'C00003'` に変えて再コンパイル・実行し、`J00004`・`J00008` の2件が印刷されることを確認してください。
2. `JUDATE` の O 仕様書の行に、編集コード `3`(38桁目に `3`)を追加してみてください。数値のままなので見た目はほぼ変わりませんが、`F4` プロンプトで編集コードの入力欄を確認する練習になります。
3. (発展)存在しない得意先コード(`'C99999'`)を指定すると、`TOKNM` は `NOTFOUND` になりますが、受注の絞り込み(`JUTOK IFEQ CUST`)には何も一致しないため、明細行が1件も印刷されません。実際に試して確認してください。

## セルフチェック

- [ ] 編集コード1〜4の違い(桁区切り・ゼロのときの表示)を説明できる。
- [ ] 複数のファイルを1つのプログラムで宣言し、`CHAIN`・`READ` をそれぞれ使い分けられた。
- [ ] `JUCINQ3`(`R0408A`)を実装し、02-06・03-09 の結果と一致することを確認できた。

## 片付け

作成したプログラムはそのまま残してください。

## まとめ

| 英語 | 日本語 |
|---|---|
| Edit code | 編集コード |
| Zero balance | ゼロ残高(値がゼロのときの表示) |

次のレッスン(04-09)では、`ZAHIK3`(在庫引当)で、更新・追加・削除とレコード・ロックを扱います。

## 実機メモ

- 確認日: 2026-09-25。**`R0408A` を `<USER>2` で実際に `CRTRPGPGM` でコンパイル(Highest Severity 00)し、実行して `ACME TRADING CO J00001`/`J00003` の2行が正しく印刷されることを確認した。**
- 編集コードの表(1〜4)は、`TOTAL`(`1738.00`)に編集コード `1`/`3` を、`ZERO`(`0.00`)に編集コード `1`/`2` を適用する実機テストで、桁区切りの有無とゼロ時の表示の違いを確認した。
- **このレッスンの検証中に、`<USER>2/JUCHUM` の実データが本来の2倍(16件)になっているという問題を発見した。** 原因は `tools/qclsrc/txsetup.clp` の設計ミスで、`FORCE(*YES)` で再実行すると、既存データを消さずに `load_v1.sql` を再投入してしまい、`UNIQUE` 制約のない `JUCHUM` のキー(`JUNO`)で重複が発生していた。**`txsetup.clp` を修正し(データ投入の直前に `reset_v1.sql` で全件削除するようにした)、`FORCE(*YES)` を2回連続実行しても重複しないことを実機で確認した。** 詳細は `docs/probes.md` を参照。この調査の過程で、`R0408A` 自体のロジックは最初から正しかったことも確認できている。
- 食い違いに気づいたら [Issue](https://github.com/bluemoonjp/scratchpad-ibmi-learning-from-zero/issues) で教えてください。
