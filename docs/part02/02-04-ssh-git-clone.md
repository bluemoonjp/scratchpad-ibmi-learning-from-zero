# 02-04 SSH・PASE と教材の git clone

> 所要時間: 60分 / 前提レッスン: 02-03 / 目標番号: 1 / 観測方法: SQL の SELECT 結果(16進は DSPPFM)/ 道具: 5250、SSH / 同時接続数: 5250 + SSH / 作る・変えるオブジェクト: `/home/<USER>/ibmi-kyozai`(このリポジトリーの clone)/ DBVER: なし / 依存するプローブ: P08, P09, P37 / PTF 依存: なし / 容量の目安: 数十 MB

## ゴール

- SSH(2222番)で PUB400 に接続できる。
- この教材のリポジトリーを、`git clone --sparse` で自分のホーム・ディレクトリーに取り込める。

## ウォームアップ

<details><summary>前回の復習</summary>

1. `CR` が1バイト混ざると何が起きる?
2. IFS のパスで、ライブラリーは何という拡張子になる?

答え: 1. 固定形式ソースの桁がずれて壊れる 2. `.LIB`

</details>

## なぜ学ぶか

この後のレッスン(02-05 以降)で使うサンプル・データベースの定義や、ツール類は、すべてこのリポジトリー(GitHub 上で公開されているこの教材そのもの)に入っています。**5250 で1行ずつ手入力するのではなく、リポジトリーを丸ごと自分の PUB400 アカウントに取り込む**のが、この先の効率的な進め方です。同時に、SSH という新しい接続方法にも慣れます。

## 新出

- PASE と QSH(5250 とは別の実行環境)
- SSH(2222番)での接続
- `git clone --sparse`・`git pull`

## 説明

### SSH は別のジョブ

これまでは 5250 の対話式ジョブだけを使ってきましたが、SSH で接続すると、**別のジョブ**が動きます。5250 のジョブと SSH のジョブは完全に独立していて、ライブラリー・リストなどの設定も共有されません。

### PASE と QSH

IBM i には、UNIX 系のプログラムを動かすための実行環境である **PASE** があります。SSH で接続すると、既定では PASE の `bsh`(シェル)が起動します。`qsh` という、より POSIX に近いシェルもあります。この教材の自動化ツール(TX 系のツール)は `qsh` 経由で CL コマンドを呼び出す設計になっています。

### 接続の作法(重要)

PUB400 は共有の練習機です(00-02 参照)。SSH 接続では、次のことを守ってください。

- **パスワード認証に2回失敗したら、それ以上リトライしない。** 設定を確認してから、時間を空けて再接続してください。認証の失敗が続くと、IP アドレスごと一時的に遮断されることがあります。
- **1回の接続で、できるだけ多くの作業をまとめる。** 接続を何度も繰り返すと、たとえ認証に一度も失敗していなくても、接続数の上限に触れることがあります。
- SSH の中では、5250 のような画面系のコマンドは使いません(動きません)。

### git が既定の PATH に入っていない

PUB400 には git が入っていますが、**SSH でログインした直後の `PATH` には含まれていません。** 実行するには、フル・パス(`/QOpenSys/pkgs/bin/git`)を指定するか、次のように `~/.profile` に1行追加してください(最初の1回だけで済みます)。

```sh
echo 'export PATH=/QOpenSys/pkgs/bin:$PATH' >> ~/.profile
```

## 実演

1. ターミナル(お使いの PC のもの。00-03 とは別に、SSH クライアントが必要です)から、次のように接続します(`<自分のユーザー名>` を置き換えてください)。

   ```sh
   ssh -p 2222 <自分のユーザー名>@pub400.com
   ```

   パスワードを尋ねられたら入力します。

2. 接続できたら、まず PATH を通します(初回のみ)。

   ```sh
   echo 'export PATH=/QOpenSys/pkgs/bin:$PATH' >> ~/.profile
   export PATH=/QOpenSys/pkgs/bin:$PATH
   ```

3. この教材のリポジトリーを、必要なディレクトリーだけに絞って取り込みます(`--sparse` で、最初は最小限だけ取得します)。

   ```sh
   git clone --filter=blob:none --sparse https://github.com/bluemoonjp/scratchpad-ibmi-learning-from-zero.git ~/ibmi-kyozai
   cd ~/ibmi-kyozai
   git sparse-checkout set src db tools
   ```

4. 取り込んだ容量を確認します。

   ```sh
   du -sh ~/ibmi-kyozai
   ```

5. `exit` で SSH セッションを終了し、5250 に戻ります。

## 同じ手順を別の対象で

1. もう一度 SSH で接続し、`cd ~/ibmi-kyozai && git pull` で更新を取り込む練習をします(今は変更がなくても、コマンドの流れを覚えることが目的です)。
2. 5250 の対話式ジョブと、SSH のジョブが別物であることを確かめます。5250 のコマンド行で `system "DSPLIB LIB(<自分のユーザー名>1) OUTPUT(*PRINT)"` を **SSH の中で** 実行し(`qsh` 経由。`ssh <ユーザー>@pub400.com /usr/bin/qsh` として、標準入力からこのコマンドを渡す方法もあります)、`WRKSPLF` を **5250 側で** 実行して、スプールがちゃんと自分のライブラリーに対して作られていることを確認してください。

## 出力が違うとき

- `git: command not found` のようなエラーが出たら、PATH が通っていません。上記の `export PATH=...` を再確認してください。
- 接続がタイムアウトする、または拒否される場合は、無理に再接続を繰り返さず、時間を空けてください。

## 演習

1. `~/ibmi-kyozai/db/v1/` の中身を `ls` で一覧し、02-02 で自分が手入力した `TOKUIM`/`SHOHIM` の DDS と見比べてください(内容が一致するはずです)。
2. SSH のセッションを終了し、5250 に戻って `DSPJOBLOG` を確認してください。SSH での操作が、5250 側のジョブ・ログには**現れない**ことを確認してください(別ジョブだからです)。

## セルフチェック

- [ ] SSH(2222番)で接続できた。
- [ ] `git clone --sparse` で教材を取り込めた。
- [ ] PASE と QSH の違いを説明できる。
- [ ] 5250 と SSH が別ジョブであることを、自分で確かめた。

## 片付け

このレッスンで取り込んだ `~/ibmi-kyozai` は、この先ずっと使うので削除しないでください。

## まとめ

| 英語 | 日本語 |
|---|---|
| PASE | PASE(UNIX系プログラムの実行環境) |
| Sparse checkout | スパース・チェックアウト(部分的な取得) |

次のレッスン(02-05)では、取り込んだ教材から `TXSETUP` を使って、サンプル・データベースを完成させます。

## 実機メモ

- 確認日: 2026-09-25(`docs/probes.md` P08 関連)。SSH(2222番)での接続方法、git が `/QOpenSys/pkgs/bin/` にあるが既定 PATH には含まれないこと、`~/.profile` での対処は、著者が実機で確認済み。
- 接続数の上限に関する記述は、このセッション中に著者自身が実際に SSH 接続不能を経験したことに基づく(`docs/probes.md` の「接続数に関する注意」参照)。
- `git clone` をこの教材自身のリポジトリー(学習者にとっての「相手」)に対して行う手順そのものは、このセッションでは実行していない(次回確認する)。
